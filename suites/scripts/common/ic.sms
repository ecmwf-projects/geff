#!/bin/bash -l

%manual

Prepare initial conditions for ecFire model

%end

%include <ecflow_bash.h>
%include <sim.h>

mkdir -p $sim_dir

if [[ $sim_cold_start -ne 1 ]]; then
    cp $sim_prev_dir/next_run_ic.nc.gz $sim_dir/ic.nc.gz
fi
