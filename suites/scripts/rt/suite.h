# ------------------------------
# suite-level parameters
# ------------------------------

# work directory of the suite
suite_dir=<?config.get('workdir')?>

# where is the static data of the suite
suite_datadir=<?config.get('datadir')?>

# GEFF experiment ID
suite_expver=<?config.get('expver')?>
